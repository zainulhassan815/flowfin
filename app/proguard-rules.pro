# Keep the public API of every Koin module declaration. Koin uses reflection-
# free DSL by default, so the default rules are sufficient for v0.

# SQLDelight does not need ProGuard rules — generated code references are
# concrete and reachable from the application's DI bindings.
